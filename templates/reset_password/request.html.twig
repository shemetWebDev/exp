{% extends 'base.html.twig' %}
{% set base = 'auth' %}

{% block title %}Восстановление пароля
{% endblock %}

{% block body %}
	<section class="{{ base }}">
		<div class="container">
			<div class="{{ base }}__card">
				<div class="{{ base }}__notice" role="note">
					<span class="{{ base }}__notice-text">Вспомнили пароль?</span>
					<a href="{{ path('app_login') }}" class="{{ base }}__notice-link">Войти</a>
				</div>

				{% for flash_error in app.flashes('reset_password_error') %}
					<div class="{{ base }}__error" role="alert">{{ flash_error }}</div>
				{% endfor %}

				<h1 class="{{ base }}__title">Восстановление пароля</h1>
				<p class="{{ base }}__hint">
					Укажите e-mail, на который зарегистрирован аккаунт. Если он есть в базе, мы отправим ссылку для сброса пароля.
				</p>

				{{ form_start(requestForm, { attr: { class: base ~ '__form', novalidate: 'novalidate' } }) }}
				{{ form_label(requestForm.email, 'E-mail', { label_attr: { class: base ~ '__label' } }) }}
				{{ form_widget(requestForm.email, { attr: {
            class: base ~ '__input',
            placeholder: 'name@email.com',
            autocomplete: 'email'
        }}) }}
				{{ form_errors(requestForm.email) }}

				<div class="{{ base }}__actions">
					<button class="btn btn--primary btn--lg" type="submit">Отправить ссылку</button>
					<a class="btn btn--soft" href="{{ path('app_login') }}">Отмена</a>
				</div>
				{{ form_end(requestForm) }}
			</div>
		</div>
	</section>
{% endblock %}
